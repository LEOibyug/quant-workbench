"""Recheck the published fixed ensemble under corrected stock-stop semantics."""

from study_adaptive_transfer import main

if __name__ == "__main__":
    main(("fixed_ensemble",), "2026-09-21-stop-fixed-ensemble.json")
